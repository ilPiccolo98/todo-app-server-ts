import { RequestHandler } from "express";

export const addActivity: RequestHandler = (req, res, next) => {};

export const deleteActivity: RequestHandler = (req, res, next) => {};

export const updateActivity: RequestHandler = (req, res, next) => {};
